# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Editors
.vscode/
.idea/
*.swp
*~

# Local tooling / scratch
.claude/
node_modules/
*.log

# Exports produced by the app itself
hisaab-*.json
hisaab-*.csv

# Private working notes. This is a public repo and info.txt carries an email address —
# ignored so that a stray "git add ." can never publish it.
info.txt
